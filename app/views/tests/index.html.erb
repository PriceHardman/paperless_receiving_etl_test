<h1>Listing tests</h1>

<table>
  <tr>
    <th>Sailing date</th>
    <th>Customer</th>
    <th>Shipper</th>
    <th>Consignee</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tests.each do |test| %>
  <tr>
    <td><%= test.sailing_date %></td>
    <td><%= test.customer %></td>
    <td><%= test.shipper %></td>
    <td><%= test.consignee %></td>
    <td><%= link_to 'Show', test %></td>
    <td><%= link_to 'Edit', edit_test_path(test) %></td>
    <td><%= link_to 'Destroy', test, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Test', new_test_path %>
